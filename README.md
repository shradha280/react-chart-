# react-chart-
BarChart and Scatter chart implementation 
Firstly I have implemented the BarChart graph between intensity of the color and hue .
Secondly, I have implemented the scatter chart graph between the concentration malic acid in alcohol
I have import the react-chartjs-2 module which helps in implementation of charts.
In this project :
firstly in this project we need to install yarn package to run the our project 
I created the react-app through this command create-react-app- This helps in to create the react app which come
Then I create a folder called reactdemo in which i create the files namely- index.js, app.js, BarChart.js,ScatterChart.js

After Completing my project 
I run the command- yarn run start which automatically runs the project in localhost:3000

